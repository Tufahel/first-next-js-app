import styles from "./Contact.module.css"

const ContactPage = () => {
    return (
        <div>
            <h1 className={styles.heading}>This is contact page/route</h1>
        </div>
    );
};

export default ContactPage;